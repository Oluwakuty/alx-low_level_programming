#include <stdio.h>
#include "main.h"

/**
 * _puts - prints a string followed by new line
 * @str: Variable characters
 * Return: Always everytime return 0
 */

void _puts(char *str)
{
	printf("%s\n", str);
}
